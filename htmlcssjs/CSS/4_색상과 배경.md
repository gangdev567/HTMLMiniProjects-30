네, 4장의 주제인 CSS의 색상과 배경에 대해 HTML과 CSS를 결합하여 여러 가지 코딩 질문을 만들어 드리겠습니다. 각 질문은 실제 코딩 과제처럼 구성되어 있으며, HTML과 CSS 코드를 작성하여 해결할 수 있습니다.

### 120. 다양한 색상 표현 방식 활용하기

다음 HTML과 CSS 코드에 다양한 색상 표현 방식을 활용하여 스타일을 적용해보세요:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>색상 표현 방식 예제</title>
    <style>
        /* 여기에 CSS 코드를 작성하세요 */
    </style>
</head>
<body>
    <div class="color-box">
        <h1>색상 표현 방식</h1>
        <div class="hex">HEX</div>
        <div class="rgb">RGB</div>
        <div class="rgba">RGBA</div>
        <div class="hsl">HSL</div>
        <div class="hsla">HSLA</div>
    </div>
</body>
</html>
```

이 코드에 다음 조건을 만족하도록 수정하세요:
- `.hex` 클래스를 가진 요소의 배경색상을 #FF0000(빨간색)으로 설정하세요.
- `.rgb` 클래스를 가진 요소의 배경색상을 rgb(0, 255, 0)(초록색)으로 설정하세요.
- `.rgba` 클래스를 가진 요소의 배경색상을 rgba(0, 0, 255, 0.5)(반투명 파란색)으로 설정하세요.
- `.hsl` 클래스를 가진 요소의 배경색상을 hsl(120, 100%, 50%)(초록색)으로 설정하세요.
- `.hsla` 클래스를 가진 요소의 배경색상을 hsla(240, 100%, 50%, 0.8)(반투명 파란색)으로 설정하세요.
- 모든 요소의 크기를 100px x 100px로 설정하고, 여백을 10px로 설정하세요.

### 121. 배경 이미지와 반복 설정 활용하기

다음 HTML과 CSS 코드에 배경 이미지와 반복 설정을 활용하여 스타일을 적용해보세요:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>배경 이미지 예제</title>
    <style>
        /* 여기에 CSS 코드를 작성하세요 */
    </style>
</head>
<body>
    <div class="background">
        <h1>배경 이미지</h1>
        <div class="no-repeat">no-repeat</div>
        <div class="repeat">repeat</div>
        <div class="repeat-x">repeat-x</div>
        <div class="repeat-y">repeat-y</div>
        <div class="center">center</div>
        <div class="cover">cover</div>
        <div class="contain">contain</div>
    </div>
</body>
</html>
```

이 코드에 다음 조건을 만족하도록 수정하세요:
- `.no-repeat` 클래스를 가진 요소의 배경 이미지를 "background.jpg"로 설정하고, 반복을 없애세요.
- `.repeat` 클래스를 가진 요소의 배경 이미지를 "pattern.png"로 설정하고, 반복을 설정하세요.
- `.repeat-x` 클래스를 가진 요소의 배경 이미지를 "texture.jpg"로 설정하고, x축으로만 반복하세요.
- `.repeat-y` 클래스를 가진 요소의 배경 이미지를 "stripes.png"로 설정하고, y축으로만 반복하세요.
- `.center` 클래스를 가진 요소의 배경 이미지를 "logo.png"로 설정하고, 중앙 정렬하세요.
- `.cover` 클래스를 가진 요소의 배경 이미지를 "cover.jpg"로 설정하고, 전체 영역을 덮도록 설정하세요.
- `.contain` 클래스를 가진 요소의 배경 이미지를 "contain.jpg"로 설정하고, 전체 영역에 맞게 조정하세요.
- 모든 요소의 크기를 200px x 200px로 설정하고, 여백을 10px로 설정하세요.

### 122. 그라데이션 효과 활용하기

다음 HTML과 CSS 코드에 그라데이션 효과를 활용하여 스타일을 적용해보세요:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>그라데이션 예제</title>
    <style>
        /* 여기에 CSS 코드를 작성하세요 */
    </style>
</head>
<body>
    <div class="gradient">
        <h1>그라데이션</h1>
        <div class="linear">linear-gradient</div>
        <div class="radial">radial-gradient</div>
        <div class="repeating-linear">repeating-linear-gradient</div>
        <div class="repeating-radial">repeating-radial-gradient</div>
        <div class="conic">conic-gradient</div>
        <div class="multiple">multiple gradients</div>
    </div>
</body>
</html>
```

이 코드에 다음 조건을 만족하도록 수정하세요:
- `.linear` 클래스를 가진 요소의 배경색상을 위에서 아래로 빨간색에서 파란색으로 그라데이션으로 설정하세요.
- `.radial` 클래스를 가진 요소의 배경색상을 중앙에서 바깥쪽으로 초록색에서 노란색으로 그라데이션으로 설정하세요.
- `.repeating-linear` 클래스를 가진 요소의 배경색상을 반복되는 선형 그라데이션으로 설정하세요.
- `.repeating-radial` 클래스를 가진 요소의 배경색상을 반복되는 방사형 그라데이션으로 설정하세요.
- `.conic` 클래스를 가진 요소의 배경색상을 원형 그라데이션으로 설정하세요.
- `.multiple` 클래스를 가진 요소의 배경색상을 여러 개의 그라데이션을 결합하여 설정하세요.
- 모든 요소의 크기를 200px x 200px로 설정하고, 여백을 10px로 설정하세요.

### 123. 복잡한 색상과 배경 활용하기

다음 HTML과 CSS 코드에 복잡한 색상과 배경을 활용하여 스타일을 적용해보세요:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>복잡한 색상과 배경 예제</title>
    <style>
        /* 여기에 CSS 코드를 작성하세요 */
    </style>
</head>
<body>
    <div class="complex-background">
        <h1>복잡한 색상과 배경</h1>
        <div class="header">Header</div>
        <div class="nav">Navigation</div>
        <div class="content">Content</div>
        <div class="sidebar">Sidebar</div>
        <div class="footer">Footer</div>
    </div>
</body>
</html>
```

이 코드에 다음 조건을 만족하도록 수정하세요:
- `.header` 클래스를 가진 요소의 배경색상을 #FF0000(빨간색)으로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.nav` 클래스를 가진 요소의 배경색상을 rgba(0, 0, 255, 0.5)(반투명 파란색)으로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- `.content` 클래스를 가진 요소의 배경색상을 hsl(120, 100%, 50%)(초록색)으로 설정하고, 글자 색상을 hsl(0, 0%, 0%)(검은색)으로 설정하세요.
- `.sidebar` 클래스를 가진 요소의 배경 이미지를 "sidebar-pattern.png"로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.footer` 클래스를 가진 요소의 배경색상을 linear-gradient(to bottom, #FF0000, #00FF00)로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- 모든 요소의 크기를 200px x 200px로 설정하고, 여백을 10px로 설정하세요.
- `.header` 요소의 글자 크기를 24px로 설정하고, 줄 간격을 1.5로 설정하세요.
- `.nav` 요소의 글자 크기를 18px로 설정하고, 줄 간격을 1.3로 설정하세요.
- `.content` 요소의 글자 크기를 16px로 설정하고, 줄 간격을 1.2로 설정하세요.
- `.sidebar` 요소의 글자 크기를 14px로 설정하고, 줄 간격을 1.1로 설정하세요.
- `.footer` 요소의 글자 크기를 12px로 설정하고, 줄 간격을 1.0로 설정하세요.

### 124. 색상과 배경의 상속과 우선순위 이해하기

다음 HTML과 CSS 코드에 색상과 배경의 상속과 우선순위를 고려하여 스타일을 적용해보세요:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>색상과 배경 상속과 우선순위 예제</title>
    <style>
        /* 여기에 CSS 코드를 작성하세요 */
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>웹 개발 블로그</h1>
        </div>
        <div class="content">
            <h2>최신 포스트</h2>
            <p>이곳에 내용이 들어갑니다.</p>
        </div>
        <div class="sidebar">
            <h3>인기 포스트</h3>
            <ul>
                <li><a href="#">포스트 1</a></li>
                <li><a href="#">포스트 2</a></li>
                <li><a href="#">포스트 3</a></li>
            </ul>
        </div>
        <div class="footer">
            <p>&copy; 2023 웹 개발 블로그</p>
        </div>
    </div>
</body>
</html>
```

이 코드에 다음 조건을 만족하도록 수정하세요:
- 모든 요소의 글자 색상을 #000000(검은색)으로 설정하고, 배경색상을 #FFFFFF(흰색)으로 설정하세요.
- `.container` 클래스를 가진 요소의 배경색상을 #F0F0F0(연한 회색)으로 설정하고, 글자 색상을 #333333(진한 회색)으로 설정하세요.
- `.container` 클래스를 가진 모든 요




---



네, 124번 문제의 나머지 부분을 계속해서 작성하겠습니다.

### 124. 색상과 배경의 상속과 우선순위 이해하기 (계속)

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>색상과 배경 상속과 우선순위 예제</title>
    <style>
        /* 여기에 CSS 코드를 작성하세요 */
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>웹 개발 블로그</h1>
        </div>
        <div class="content">
            <h2>최신 포스트</h2>
            <p>이곳에 내용이 들어갑니다.</p>
        </div>
        <div class="sidebar">
            <h3>인기 포스트</h3>
            <ul>
                <li><a href="#">포스트 1</a></li>
                <li><a href="#">포스트 2</a></li>
                <li><a href="#">포스트 3</a></li>
            </ul>
        </div>
        <div class="footer">
            <p>&copy; 2023 웹 개발 블로그</p>
        </div>
    </div>
</body>
</html>
```

이 코드에 다음 조건을 만족하도록 수정하세요:
- 모든 요소의 글자 색상을 #000000(검은색)으로 설정하고, 배경색상을 #FFFFFF(흰색)으로 설정하세요.
- `.container` 클래스를 가진 요소의 배경색상을 #F0F0F0(연한 회색)으로 설정하고, 글자 색상을 #333333(진한 회색)으로 설정하세요.
- `.container` 클래스를 가진 모든 요소의 모든 자식 `<div>` 요소의 배경색상을 #E0E0E0(중간 회색)으로 설정하고, 글자 색상을 #333333(진한 회색)으로 설정하세요.
- `.header` 클래스를 가진 요소의 배경색상을 #4CAF50(초록색)으로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.sidebar` 클래스를 가진 요소의 배경 이미지를 "sidebar-pattern.png"로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.footer` 클래스를 가진 요소의 배경색상을 linear-gradient(to bottom, #FF0000, #00FF00)로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- `#main-content` ID를 가진 요소의 배경색상을 #00FF00(초록색)으로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- `.title` 클래스를 가진 모든 요소의 배경색상을 #FFFF00(노란색)으로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- `.subtitle` 클래스를 가진 모든 요소의 배경색상을 #FF00FF(보라색)으로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- 모든 요소의 크기를 200px x 200px로 설정하고, 여백을 10px로 설정하세요.
- `.header` 요소의 글자 크기를 24px로 설정하고, 줄 간격을 1.5로 설정하세요.
- `.content` 요소의 글자 크기를 16px로 설정하고, 줄 간격을 1.2로 설정하세요.
- `.sidebar` 요소의 글자 크기를 14px로 설정하고, 줄 간격을 1.1로 설정하세요.
- `.footer` 요소의 글자 크기를 12px로 설정하고, 줄 간격을 1.0로 설정하세요.

### 125. 복잡한 색상과 배경의 상속과 우선순위 활용하기

다음 HTML과 CSS 코드에 복잡한 색상과 배경의 상속과 우선순위를 고려하여 스타일을 적용해보세요:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>복잡한 색상과 배경 상속과 우선순위 예제</title>
    <style>
        /* 여기에 CSS 코드를 작성하세요 */
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>웹 개발 포트폴리오</h1>
            <nav>
                <ul>
                    <li><a href="#">홈</a></li>
                    <li><a href="#">프로젝트</a></li>
                    <li><a href="#">스킬</a></li>
                    <li><a href="#">연락처</a></li>
                </ul>
            </nav>
        </div>
        <div class="content">
            <h2>최신 포스트</h2>
            <p>이곳에 내용이 들어갑니다.</p>
        </div>
        <div class="sidebar">
            <h3>인기 포스트</h3>
            <ul>
                <li><a href="#">포스트 1</a></li>
                <li><a href="#">포스트 2</a></li>
                <li><a href="#">포스트 3</a></li>
            </ul>
        </div>
        <div class="footer">
            <p>&copy; 2023 웹 개발 포트폴리오</p>
        </div>
    </div>
</body>
</html>
```

이 코드에 다음 조건을 만족하도록 수정하세요:
- 모든 요소의 글자 색상을 #000000(검은색)으로 설정하고, 배경색상을 #FFFFFF(흰색)으로 설정하세요.
- `.container` 클래스를 가진 요소의 배경색상을 #F0F0F0(연한 회색)으로 설정하고, 글자 색상을 #333333(진한 회색)으로 설정하세요.
- `.container` 클래스를 가진 모든 요소의 모든 자식 `<div>` 요소의 배경색상을 #E0E0E0(중간 회색)으로 설정하고, 글자 색상을 #333333(진한 회색)으로 설정하세요.
- `.header` 클래스를 가진 요소의 배경색상을 #4CAF50(초록색)으로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.nav` 클래스를 가진 요소의 배경색상을 rgba(0, 0, 255, 0.5)(반투명 파란색)으로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- `.content` 클래스를 가진 요소의 배경색상을 hsl(120, 100%, 50%)(초록색)으로 설정하고, 글자 색상을 hsl(0, 0%, 0%)(검은색)으로 설정하세요.
- `.sidebar` 클래스를 가진 요소의 배경 이미지를 "sidebar-pattern.png"로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.footer` 클래스를 가진 요소의 배경색상을 linear-gradient(to bottom, #FF0000, #00FF00)로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- `.button` 클래스를 가진 모든 요소의 배경색상을 radial-gradient(circle, #FF0000, #00FF00)로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.overlay` 클래스를 가진 모든 요소의 배경색상을 rgba(0, 0, 0, 0.5)로 설정하고, 글자 색상을 #FFFFFF(흰색)으로 설정하세요.
- `.highlight` 클래스를 가진 모든 요소의 배경색상을 #FFFF00(노란색)으로 설정하고, 글자 색상을 #000000(검은색)으로 설정하세요.
- 모든 요소의 크기를 200px x 200px로 설정하고, 여백을 10px로 설정하세요.
- `.header` 요소의 글자 크기를 24px로 설정하고, 줄 간격을 1.5로 설정하세요.
- `.nav` 요소의 글자 크기를 18px로 설정하고, 줄 간격을 1.3로 설정하세요.
- `.content` 요소의 글자 크기를 16px로 설정하고, 줄 간격을 1.2로 설정하세요.
- `.sidebar` 요소의 글자 크기를 14px로 설정하고, 줄 간격을 1.1로 설정하세요.
- `.footer` 요소의 글자 크기를 12px로 설정하고, 줄 간격을 1.0로 설정하세요.

이러한 코딩 질문들은 CSS의 색상과 배경 기능들을 다양하게 활용하는 방법을 보여줍니다. 학생들은 실제 웹 페이지에서 색상과 배경을 효과적으로 사용하는 방법을 학습할 수 있으며, 이를 통해 웹 페이지의 시각적 디자인과 사용자 경험을 개선할 수 있습니다. 다양한 색상 표현 방식, 배경 이미지 설정, 그라데이션 효과 등을 이해하고, 이를 실제 상황에 적용하는 방법을 익힐 수 있습니다.